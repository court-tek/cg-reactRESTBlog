import React, { Component } from "react";

const Footer = () => {
      return (
          <section className="footer">
              <div className="ui container">This is the footer</div>
          </section>
      );
  }

export default Footer;
